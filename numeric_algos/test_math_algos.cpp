#include <iostream>
#include "math_algos.h"
#include <cstdlib>
#include <cmath>
#include <chrono>
using namespace std;

int main(){
	int i = 0;
	cin >> i;
	
	while (i != -1){
		
		//FIXME
		/*auto t0 = chrono::high_resolution_clock::now();
		cout << factorial_head_recursive(i) << endl;
		auto t1 = chrono::high_resolution_clock::now();
		cout << "Factorial, head recursive implementation: " << chrono::duration_cast<chrono::nanoseconds>(t1 - t0).count() 
			<< " nanoseconds passed\n";
	
		auto t2 = chrono::high_resolution_clock::now();
		cout << factorial_tail_recursive(i, 1) << endl;
		auto t3 = chrono::high_resolution_clock::now();
		cout << "Factorial, tail recursive implementation: " << chrono::duration_cast<chrono::nanoseconds>(t3 - t2).count() 
			<< " nanoseconds passed\n";
		
		
		auto t4 = chrono::high_resolution_clock::now();
		cout << factorial_iterative(i) << endl;
		auto t5 = chrono::high_resolution_clock::now();
		cout << "Factorial, iterative implementation: " << chrono::duration_cast<chrono::nanoseconds>(t5 - t4).count() 
			<< " nanoseconds passed\n";
		
		cin >> i;*/
	}
}